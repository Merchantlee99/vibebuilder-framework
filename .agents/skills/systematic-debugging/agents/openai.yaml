interface:
  display_name: "Systematic Debugging"
  short_description: "Find root cause before patching bugs"
  default_prompt: "Use $systematic-debugging to reproduce this issue, isolate the most likely root cause, apply the smallest fix, and verify the result before moving on."

policy:
  allow_implicit_invocation: true
